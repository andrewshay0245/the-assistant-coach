{
  "name": "tac-email-scripts",
  "version": "1.0.0",
  "description": "Email outreach scripts for The Assistant Coach",
  "scripts": {
    "send": "npx ts-node send-beta-invites.ts",
    "send:dry-run": "npx ts-node send-beta-invites.ts --dry-run"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.515.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
